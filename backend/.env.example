# Server Config
PORT=8080

# Database Config (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=pramukacat
DB_SSLMODE=disable

# Redis Config
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Jaeger — OpenTelemetry OTLP gRPC Endpoint
# Jaeger harus jalan di port 4317 (jalankan: make infra-up)
OTEL_EXPORTER_OTLP_ENDPOINT=localhost:4317

# SMTP / Email Config
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=email.anda@gmail.com
SMTP_PASS=app_password_anda_dari_google
SMTP_SENDER="Pramuka CAT <email.anda@gmail.com>"
